# This file intentionally left minimal.
# The PyMesh::Wires target is defined in tools/Wires/CMakeLists.txt,
# which is processed before this directory via the root CMakeLists.txt.
# This stub exists solely to satisfy the add_subdirectory() call in
# the root CMakeLists.txt.
